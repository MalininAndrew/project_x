services:
  postgres:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: andrew_malinin
      POSTGRES_PASSWORD: s5ixGvXeyi
      POSTGRES_DB: project_x_db
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - project_x

volumes:
  pgdata:

networks:
  project_x:
    driver: bridge